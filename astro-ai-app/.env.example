# Use this to override the API URL used by the app (recommended for physical device testing).
# Replace with your laptop's LAN IP from `ipconfig`.
# Example: http://192.168.1.5:8000
EXPO_PUBLIC_API_URL=http://10.120.106.141:8000
